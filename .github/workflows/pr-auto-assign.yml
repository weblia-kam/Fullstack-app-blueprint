name: PR Auto Assign

on:
  pull_request:

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v2
        with:
          reviewers: kyrreMartila
          assignees: kyrreMartila
          addReviewers: true
          addAssignees: true
